[workspace]
members = [
    "rust-lib-core/lib",
    "rust-web-axum",
    "registry-tui",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
